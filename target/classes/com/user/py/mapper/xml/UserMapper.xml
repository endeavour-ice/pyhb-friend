<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.user.py.mapper.UserMapper">

    <resultMap id="resultUserAvatarVo" type="com.user.py.mode.domain.vo.UserAvatarVo">
        <id property="id" column="id"/>
        <result property="username" column="user_account"/>
        <result property="avatarUrl" column="avatar_url"/>
    </resultMap>
    <select id="getUserAvatarVoByIds" resultMap="resultUserAvatarVo">
        select id,user_account, avatar_url from `user` where id in (${ids});
    </select>
</mapper>
